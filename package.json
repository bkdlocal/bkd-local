{
  "name": "bkd-local",
  "version": "0.1.0",
  "private": true,
  "description": "Bkd Local — baker-facing mobile web app",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "multer": "^2.2.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
